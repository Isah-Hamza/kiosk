const BASE_URL = "https://https://service.glowbiz.ng/api/v1";

const ApiEndPoints = {
  category: "User/user-detail-info",

  // USER
  authenticate: "User/authenticate",
};

export { ApiEndPoints, BASE_URL };
